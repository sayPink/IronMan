import routers from '../../router/router'

const state = {
  routers
}

const mutations = {
  // 
}

const actions = {
  //
}

export default {
  //命名空间  namespaced: true,
  namespaced: true,
  state,
  mutations,
  actions
}
